SELECT
    I.ANIMAL_ID,
    I.NAME
FROM
    ANIMAL_INS AS I
JOIN
    ANIMAL_OUTS AS O
    ON
        O.ANIMAL_ID = I.ANIMAL_ID
GROUP BY
    ANIMAL_ID
ORDER BY
    DATEDIFF(I.DATETIME, O.DATETIME)
LIMIT
    2;
